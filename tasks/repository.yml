---
## @see https://github.com/nodesource/distributions
## @see https://deb.nodesource.com/setup_lts.x
- name: Install dependency packages
  ansible.builtin.apt:
    update_cache: true
    name:
      - apt-transport-https
      - ca-certificates
      - curl
      - gnupg
- name: Add apt signing key
  ansible.builtin.get_url:
    url: "{{ nodejs_apt_key_url }}"
    dest: "{{ nodejs_apt_key_download_dest }}"
    mode: "0644"
  register: nodejs_key_download_result
- name: Add an Apt signing key, uses whichever key is at the URL
  ansible.builtin.apt_key:
    url: "{{ nodejs_apt_key_url }}"
    state: present
- name: Convert key
  ansible.builtin.command:
    cmd: gpg --dearmor -o {{ nodejs_apt_key_dest }} {{ nodejs_apt_key_download_dest }}
  when: nodejs_key_download_result is changed
- name: Check architecture
  ansible.builtin.command:
    cmd: dpkg --print-architecture
  changed_when: false
  register: dpkg_result
- name: Set arch variables
  ansible.builtin.set_fact:
    nodejs_arch: "{{ dpkg_result.stdout }}"
- name: Add nodesource repository
  ansible.builtin.apt_repository:
    filename: nodesource
    repo: deb [arch={{ nodejs_arch }} signed-by={{ nodejs_apt_key_dest }}] https://deb.nodesource.com/node_{{ nodejs_version }}.x nodistro main
    state: present
- name: Add N|solid Config
  ansible.builtin.blockinfile:
    path: /etc/apt/preferences.d/nsolid
    marker: "# {mark} N|solid ANSIBLE MANAGED BLOCK"
    content: |
      Package: nsolid
      Pin: origin deb.nodesource.com
      Pin-Priority: 600
    mode: "0644"
- name: Add Nodejs Config
  ansible.builtin.blockinfile:
    path: /etc/apt/preferences.d/nodejs
    marker: "# {mark} nodejs ANSIBLE MANAGED BLOCK"
    content: |
      Package: nodejs
      Pin: origin deb.nodesource.com
      Pin-Priority: 600
    mode: "0644"
